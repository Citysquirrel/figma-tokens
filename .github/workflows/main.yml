on:
  push:
    branches:
    - main
jobs:
  my_job:
    name: excute transfomer
    runs-on: ubuntu-18.04
    
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 16
    - name: npm runs
      run: |
        npm install
        npm run tf
        npm start
      
